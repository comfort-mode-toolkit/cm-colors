name: Open Journals PDF Generator

on:
  push:
    branches:
      - paper

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate PDF
        uses: openjournals/open-journal-draft-action@v1.0
        # with:
        #   paper-path: 'paper.md'
        #   journal: 'joss'
